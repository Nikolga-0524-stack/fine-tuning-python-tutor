# 🤖 Fine-tuning GPT-2 для AI-репетитора по Python

## 📋 Описание проекта

Проект демонстрирует процесс дообучения (fine-tuning) языковой модели GPT-2 для создания AI-репетитора, который отвечает на вопросы начинающих программистов по языку Python.

### 🎯 Бизнес-задача
Разработать AI-ассистента, способного отвечать на типовые вопросы о переменных, условных операторах, списках и других базовых концепциях Python.

## 🛠 Технологический стек

- **Python 3.12**
- **HuggingFace Transformers** — работа с моделями
- **PyTorch** — фреймворк для обучения
- **Google Colab** — облачная среда с GPU (T4)
- **Full Fine-Tuning** — метод дообучения

## 📦 Датасет

- **Размер:** 87 примеров (69 train / 18 test)
- **Формат:** instruction → output (JSON)
- **Тематика:** базовые вопросы по Python

**Пример:**
```json
{
  "instruction": "Что такое переменная в Python?",
  "output": "Переменная в Python - это именованная область памяти..."
}
Эксперимент 1: Дообучение английской GPT-2 на русском датасете
Параметры:
Модель: gpt2 (124M параметров)
Эпохи: 3
Batch size: 2 (gradient accumulation × 4)
Learning rate: 2e-4
Max length: 512 токенов
Результаты обучения:
Шаг
Training Loss
5
2.8757
10
2.3048
15
1.9832
20
1.8804
25
1.7791
Вывод:
✅ Loss снизился на 38% — модель обучалась
❌ При генерации модель выдавала бессвязный текст
Причина: Английский токенизатор GPT-2 неэффективен для русского языка — разбивает слова на байты/символы, что разрушает семантику.
Эксперимент 2: Русская модель sberbank-ai/rugpt2small
Результат: Модель переведена в статус gated (закрыта). Требует регистрации и одобрения от авторов.
Эксперимент 3: Большая русская модель rugpt3large
Результат: Превышен лимит RAM в Google Colab (12GB). Модель слишком большая для бесплатной версии.
💡 Ключевые выводы
Язык модели должен совпадать с языком данных — токенизатор критически важен
Низкий loss ≠ качественная генерация — нужны метрики BLEU/ROUGE
Доступность моделей — всегда проверяйте лицензию и gated-статус
Ресурсы имеют значение — большие модели требуют мощного железа
LoRA/QLoRA — необходим для экономии памяти при работе с большими моделями
🚀 Как запустить
Откройте Google Colab
Загрузите файл train_dataset.json
Выполните код обучения (см. inference/chat.py)
📈 Следующие шаги
Использовать русскую модель с открытым доступом (Saiga, ruT5)
Добавить метрики BLEU/ROUGE для количественной оценки
Применить LoRA для экономии памяти
Увеличить датасет до 500+ примеров
Создать Gradio-интерфейс для демонстрации
👤 Автор
Проект выполнен в рамках обучения fine-tuning языковых моделей.
